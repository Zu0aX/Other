local remoteFolder = game:GetService("ReplicatedStorage").DefinEvents

local remote1 = remoteFolder:FindFirstChild("InstancePropertyFunction")
if remote1 then
    remote1.Name = "Zu0aXFunction"
end

local remote2 = remoteFolder:FindFirstChild("InstanceReplicateFunction")
if remote2 then
    remote2.Name = "Zu0aXFunction2"
end
